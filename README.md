# Leve Panel

A very simple and lightweight panel for Wayland. It uses pure Wayland and doesn't have any heavy dependencies like Qt or GTK. It only needs `wlr-layer-shell` installed. So it should work on any compositor that supports the `wlr-layer-shell-unstable-v1` protocol.

![leve_panel](https://github.com/BigMacTaylor/leve-panel/blob/main/screenshots/leve_panel.png "Leve Panel")
